# Flask GraphQL Starter-kit

This starter-kit is a simple flask project for getting started Flask application with GraphQL support.


## Required

- Docker v1.3 and above

## Project setup

```
$ git clone https://github.com/pycodersvn/flask-graphgl-staterkit
$ cd flask-graphgl-staterkit
$ docker-compose up
```